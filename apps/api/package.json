{
  "name": "@coffeelot/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@coffeelot/shared": "workspace:*",
    "@prisma/client": "latest",
    "elysia": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
